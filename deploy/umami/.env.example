# Copy to .env on the VPS and replace placeholders before `docker compose up -d`.
# Generate secrets: openssl rand -hex 32

UMAMI_DB_PASSWORD=change-me-umami-db-password
UMAMI_APP_SECRET=change-me-to-a-long-random-string-at-least-32-chars
