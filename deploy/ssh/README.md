# SSH keys for VPS deploy

Public keys only — safe to commit.

| File | Use |
|------|-----|
| `hosterby-vps.pub` | Hosterby VPS (Drift Index + other pet projects) |

Add in Hosterby panel: **Server → SSH keys** → paste full line from the file.
