{
  "name": "drift-index",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=apps/api & npm run dev --workspace=apps/web",
    "dev:api": "npm run dev --workspace=apps/api",
    "dev:web": "npm run build --workspace=packages/shared && npm run dev --workspace=apps/web",
    "build": "npm run build --workspace=packages/shared && npm run build --workspace=apps/api && npm run build --workspace=apps/web",
    "db:up": "docker compose up -d",
    "db:down": "docker compose down",
    "db:migrate": "npm run db:migrate --workspace=apps/api",
    "db:migrate:deploy": "npm run db:migrate:deploy --workspace=apps/api",
    "db:seed": "npm run db:seed --workspace=apps/api",
    "db:import:royal-ds": "npm run db:import:royal-ds --workspace=apps/api",
    "db:setup": "npm run db:up && npm run db:migrate && npm run db:seed && npm run db:import:royal-ds",
    "db:setup:prod": "npm run db:migrate:deploy && npm run db:seed && npm run db:import:royal-ds"
  }
}
